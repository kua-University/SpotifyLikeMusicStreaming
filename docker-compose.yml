services:
  spotify-prototype:
    build: .
    container_name: spotify-like-prototype
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    volumes:
      - ./data:/app/data
